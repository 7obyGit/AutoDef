# AutoDef

Use LLMs for functions

## TODO

- Concept of e.g. "@AutoDef" -> generate function body at runtime based on description using llm
- Concept of e.g. "@Func" -> all queries are passed directly to LLMs and the result is returned from an LLM
